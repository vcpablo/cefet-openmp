#include<stdio.h>
//#include<conio.h>
#include<stdlib.h>
#include<time.h>
/*int main(){
    long  n = 100000;
    long array[n],c,d,swap;
   // printf("Informe a quantidade de elementos:\n ");
    //scanf("%ld", &n);

        srand(time(NULL));
    printf("Preenchendo o vetor com valores\n");
    for (c = 0;c<n;c++){
        array[c] = rand()%100000;
      //  printf(" posicao %ld : %ld\n",c,array[c]);
    }
    bubble_sort(array,n);

    printf("Ordenado por ordem ascendente\n");
     for ( c = 0 ; c < n ; c++ )
     printf(" posicao %ld : %ld\n",c,array[c]);

    return 0;

}

    void bubble_sort(long array[], long n){
        long c,d,t;
        for(c=0;c<(n-1); c++){
            for(d=0;d<n-c-1;d++){
                if(array[d]>array[d+1]){
                    t = array[d];
                    array[d] = array[d+1];
                    array[d+1] = t;
                }
            }

        }

    }
*/
